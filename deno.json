{
  "tasks": {
    "dev": "deno run --allow-net --allow-env --unstable-cron --unstable-kv main.ts",
    "scan": "deno eval --allow-net --allow-env \"const r=await fetch('http://localhost:8000/scan');console.log(await r.text())\"",
    "dogrula": "deno run --allow-net --allow-env dogrula.ts",
    "check": "deno check main.ts dogrula.ts",
    "fmt": "deno fmt",
    "lint": "deno lint"
  },
  "fmt": {
    "lineWidth": 100
  },
  "lock": false
}
